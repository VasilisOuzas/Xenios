# Progress (Version 0.2)
> This is an experimental version of the application. It is not optimized, polished or properly tested yet. Use it with your own resonsibility
---
This is the second unofficial version of the application. It is not woring properly yet. It implements a more advanced GUI than version one for checking availability through specific dates and booking reservations.
The data input/output stream is hosted via .json files that are saved in the `/data` directory.

# Fixes/Upgrades over prior Version 0.1
- The design format got changed to not include year, for simplicity reasons
- Adding reservations now works
- The GUI got a visual and function upgrade
- There is now a calendar to help the user identify reservations with a faster/simpler way
- 



 # Issues 
 - the dating format needs to be changed (-)
 - no logo/repo links
 - rooms.json only has 5 registries (which is not aligned with the code)
 - Single rooms should be replaced with double rooms
 - The colors are not correlated
 - Checking for a room gives me the available double/triple/quadruple rooms even though on the .json file are only double/single/suits and for that reason the program gives 0 of that rooms that are available.  
- I NEED TO UPDATE THE .JSON
